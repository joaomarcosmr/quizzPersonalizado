import { Question } from "../../../interface/questions";

type Props = {
  currentQuestion: Question;
};

const QuizzInfo = ({ currentQuestion }: Props) => {
  return (
    <div>
      <p>Quizz</p>
    </div>
 )
};

export default QuizzInfo;
